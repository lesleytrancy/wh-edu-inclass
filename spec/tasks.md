# 武侯课教智慧课堂 · 文档补齐任务

更新日期：2026-09-15。本清单只跟踪本次文档补齐，不将历史实现或原始需求缺口列为待开发任务。

## 准备与基础

- [x] [T001] 核对项目指令、现有文档、依赖、源码与测试：`requierment.md`、`package.json`、`src/`、`tests/`、`tools/`。
- [x] [T002] 建立现有功能规格、用户场景、可验收要求与原始需求差距：`spec/spec.md`。
- [x] [T003] 记录实际架构、状态、存储、依赖、文件结构和文档实现方案：`spec/plan.md`。
- [x] [T004] 建立按依赖顺序排列的文档实施与验收清单：`spec/tasks.md`。

## US-01 · 首次运行与三端演示（P1）

- [x] [T005] 补齐环境要求、安装/启动/测试/构建命令、三端入口和演示账号：`README.md`。
- [x] [T006] 补齐上传、练习、提问、小组讨论、地理工具、结束课堂与报告演示步骤：`README.md`。

## US-02 · 维护与能力边界（P2）

- [x] [T007] 补齐目录职责、本地存储、数据重置、浏览器要求、模拟能力及文档链接：`README.md`。

## 校验与交付

- [x] [T008] 检查四份文档的相对链接、入口、账号、存储键、命令和能力描述与源码一致：`README.md`、`spec/spec.md`、`spec/plan.md`、`spec/tasks.md`。
- [x] [T009] 执行现有自动测试并验证生产构建，使用临时构建目录保护已有 `dist/`，记录结果：`tests/`、`package.json`、`spec/tasks.md`。
- [x] [T010] 核对所有任务状态及变更范围，交付四份文档：`README.md`、`spec/`。

依赖：T001 → T002 → T003 → T004 → T005 → T006 → T007 → T008 → T009 → T010。

## 验证记录

2026-09-15，Node 20.20.2 / npm 10.8.2：

- 文档静态检查通过：四份文档的本地链接可解析、代码围栏闭合，三端入口、初始账号、存储键及 npm 脚本与源码一致；规格包含 19 条 FR，任务包含 10 个 TaskID。
- `npm test` 通过：3 个测试文件，3 通过、0 失败，分别为学习逻辑、讨论逻辑、讨论与课堂关键 JSX 渲染。
- `npm run build -- --outDir /tmp/wh-edu-doccheck.rbZIVo` 成功：25 个模块完成转换。输出到全新临时目录，未改写已有 `dist/`。
- 构建提示输出目录在项目外，以及地理工具块超过 500kB；均为非阻断提示，不影响本次文档交付。
- 浏览器多标签同步、IndexedDB 文件读写、麦克风授权、PDF 查看与打印本次未重新执行，保留为手动验收项目。
- 本次没有执行 `npm ci` 或更新依赖；测试与构建使用当前已安装依赖，版本已与锁文件核对。
- 最终变更范围核对通过：本次新增 `README.md` 与 `spec/` 下三份文档，未修改应用、依赖及已有文件；工作区另有上次生成的未跟踪 `DESIGN.md`。10 项任务全部完成。
